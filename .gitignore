# this is generated by running `cargo run -- --help-markdown > CLI-help.md`
# but we should just copy/paste it into README.md so we ignore it
CLI-help.md

# surveilr testing
device-surveillance.sqlite.db*

# rust-specific
/target
